node_modules/
.env
*.tmp
# Migration SQL files are needed in Docker image at runtime
# drizzle/*.sql

docker-compose.override.yml
admin/dist

.impeccable/

# Playwright
admin/test-results/
admin/playwright-report/
